-- // Akiri UI Library \\ --
local UI = loadstring(game:HttpGet("https://akiri.best/assets/files/gayasf.ui2?key=5y1lxXSfWKhlQkSqhUuFyB8kPp8hsCau"))()

-- // Main Window \\ --
local Window = UI:Window("Test Cheat Menu", Vector2.new(500, 600))

-- // Tabs \\ --
local CombatTab = Window:Tab("Combat")
local MovementTab = Window:Tab("Movement")
local VisualsTab = Window:Tab("Visuals")
local SettingsTab = Window:Tab("Settings")

-- // Services \\ --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

-- // Variables \\ --
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

-- // Settings \\ --
local Settings = {
    SpeedHack = {
        Enabled = false,
        Multiplier = 2,
        OriginalWalkSpeed = 16,
        OriginalJumpPower = 50
    },
    Fly = {
        Enabled = false,
        Speed = 100,
        BodyVelocity = nil,
        Connection = nil
    },
    Noclip = {
        Enabled = false
    },
    InfiniteJump = {
        Enabled = false,
        JumpPower = 50
    },
    Aimbot = {
        Enabled = false,
        FOV = 100,
        Smoothness = 10,
        TeamCheck = true,
        VisibleCheck = true,
        TargetPart = "Head"
    },
    ESP = {
        Enabled = false,
        TeamCheck = true,
        ShowNames = true,
        ShowDistance = true,
        ShowHealth = true
    }
}

-- // Get Character \\ --
local function GetCharacter()
    return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
end

local Character = GetCharacter()
local Humanoid = Character:WaitForChild("Humanoid")
local RootPart = Character:WaitForChild("HumanoidRootPart")

-- // Save Original Properties \\ --
Settings.SpeedHack.OriginalWalkSpeed = Humanoid.WalkSpeed
Settings.SpeedHack.OriginalJumpPower = Humanoid.JumpPower

-- // Combat Tab \\ --
local AimbotSection = CombatTab:Section("Aimbot", "Left")
local ESPSection = CombatTab:Section("ESP", "Right")

-- Aimbot Toggle
AimbotSection:Toggle({
    Title = "Enable Aimbot",
    Callback = function(State)
        Settings.Aimbot.Enabled = State
    end
})

-- Aimbot FOV
AimbotSection:Slider({
    Title = "Aimbot FOV",
    Default = 100,
    Min = 10,
    Max = 500,
    Callback = function(Value)
        Settings.Aimbot.FOV = Value
    end
})

-- Aimbot Smoothness
AimbotSection:Slider({
    Title = "Smoothness",
    Default = 10,
    Min = 1,
    Max = 30,
    Callback = function(Value)
        Settings.Aimbot.Smoothness = Value
    end
})

-- Team Check
AimbotSection:Toggle({
    Title = "Team Check",
    Default = true,
    Callback = function(State)
        Settings.Aimbot.TeamCheck = State
    end
})

-- Visible Check
AimbotSection:Toggle({
    Title = "Visible Check",
    Default = true,
    Callback = function(State)
        Settings.Aimbot.VisibleCheck = State
    end
})

-- Target Part
AimbotSection:Dropdown({
    Title = "Target Part",
    List = {"Head", "HumanoidRootPart", "Torso"},
    Default = "Head",
    Callback = function(Value)
        Settings.Aimbot.TargetPart = Value
    end
})

-- ESP Toggle
ESPSection:Toggle({
    Title = "Enable ESP",
    Callback = function(State)
        Settings.ESP.Enabled = State
        if State then
            CreateESP()
        else
            ClearESP()
        end
    end
})

-- ESP Team Check
ESPSection:Toggle({
    Title = "Team Check",
    Default = true,
    Callback = function(State)
        Settings.ESP.TeamCheck = State
        ClearESP()
        if Settings.ESP.Enabled then
            CreateESP()
        end
    end
})

-- Show Names
ESPSection:Toggle({
    Title = "Show Names",
    Default = true,
    Callback = function(State)
        Settings.ESP.ShowNames = State
    end
})

-- Show Distance
ESPSection:Toggle({
    Title = "Show Distance",
    Default = true,
    Callback = function(State)
        Settings.ESP.ShowDistance = State
    end
})

-- Show Health
ESPSection:Toggle({
    Title = "Show Health",
    Default = true,
    Callback = function(State)
        Settings.ESP.ShowHealth = State
    end
})

-- // Movement Tab \\ --
local SpeedSection = MovementTab:Section("Speed", "Left")
local FlySection = MovementTab:Section("Flight", "Right")

-- SpeedHack Toggle
SpeedSection:Toggle({
    Title = "Speed Hack",
    Callback = function(State)
        Settings.SpeedHack.Enabled = State
        if State then
            Humanoid.WalkSpeed = Settings.SpeedHack.OriginalWalkSpeed * Settings.SpeedHack.Multiplier
            Humanoid.JumpPower = Settings.SpeedHack.OriginalJumpPower * 1.5
        else
            Humanoid.WalkSpeed = Settings.SpeedHack.OriginalWalkSpeed
            Humanoid.JumpPower = Settings.SpeedHack.OriginalJumpPower
        end
    end
})

-- Speed Multiplier
SpeedSection:Slider({
    Title = "Speed Multiplier",
    Default = 2,
    Min = 1,
    Max = 5,
    Callback = function(Value)
        Settings.SpeedHack.Multiplier = Value
        if Settings.SpeedHack.Enabled then
            Humanoid.WalkSpeed = Settings.SpeedHack.OriginalWalkSpeed * Value
        end
    end
})

-- Infinite Jump
SpeedSection:Toggle({
    Title = "Infinite Jump",
    Callback = function(State)
        Settings.InfiniteJump.Enabled = State
        if State then
            Humanoid.JumpPower = Settings.InfiniteJump.JumpPower
        else
            Humanoid.JumpPower = Settings.SpeedHack.OriginalJumpPower
        end
    end
})

-- Jump Power
SpeedSection:Slider({
    Title = "Jump Power",
    Default = 50,
    Min = 10,
    Max = 100,
    Callback = function(Value)
        Settings.InfiniteJump.JumpPower = Value
        if Settings.InfiniteJump.Enabled then
            Humanoid.JumpPower = Value
        end
    end
})

-- Noclip
SpeedSection:Toggle({
    Title = "Noclip",
    Callback = function(State)
        Settings.Noclip.Enabled = State
        if not State then
            -- Restore collision
            for _, part in pairs(Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
})

-- Fly Toggle
FlySection:Toggle({
    Title = "Enable Fly",
    Callback = function(State)
        Settings.Fly.Enabled = State
        UpdateFly()
    end
})

-- Fly Speed
FlySection:Slider({
    Title = "Fly Speed",
    Default = 100,
    Min = 50,
    Max = 200,
    Callback = function(Value)
        Settings.Fly.Speed = Value
    end
})

-- // Visuals Tab \\ --
local CameraSection = VisualsTab:Section("Camera", "Left")
local WorldSection = VisualsTab:Section("World", "Right")

-- FOV Changer
CameraSection:Slider({
    Title = "Camera FOV",
    Default = 70,
    Min = 30,
    Max = 120,
    Callback = function(Value)
        Camera.FieldOfView = Value
    end
})

-- Third Person
CameraSection:Toggle({
    Title = "Third Person",
    Callback = function(State)
        if State then
            LocalPlayer.CameraMaxZoomDistance = 50
            LocalPlayer.CameraMinZoomDistance = 0
        else
            LocalPlayer.CameraMaxZoomDistance = 0
        end
    end
})

-- Fullbright
WorldSection:Toggle({
    Title = "Fullbright",
    Callback = function(State)
        if State then
            game:GetService("Lighting").GlobalShadows = false
        else
            game:GetService("Lighting").GlobalShadows = true
        end
    end
})

-- // Settings Tab \\ --
local ConfigSection = SettingsTab:Section("Configuration", "Left")
local InfoSection = SettingsTab:Section("Information", "Right")

-- Unload Cheat
ConfigSection:Button({
    Title = "Unload Cheat",
    Callback = function()
        -- Restore all settings
        Settings.SpeedHack.Enabled = false
        Settings.Fly.Enabled = false
        Settings.Noclip.Enabled = false
        Settings.InfiniteJump.Enabled = false
        Settings.Aimbot.Enabled = false
        Settings.ESP.Enabled = false
        
        -- Restore original properties
        Humanoid.WalkSpeed = Settings.SpeedHack.OriginalWalkSpeed
        Humanoid.JumpPower = Settings.SpeedHack.OriginalJumpPower
        
        -- Restore collision
        for _, part in pairs(Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = true
            end
        end
        
        -- Clear ESP
        ClearESP()
        
        -- Destroy fly objects
        if Settings.Fly.BodyVelocity then
            Settings.Fly.BodyVelocity:Destroy()
            Settings.Fly.BodyVelocity = nil
        end
        
        if Settings.Fly.Connection then
            Settings.Fly.Connection:Disconnect()
            Settings.Fly.Connection = nil
        end
        
        -- Self destruct UI
        UI:SelfDestruct()
    end
})

-- Info
InfoSection:Label("Cheat Loaded Successfully!")
InfoSection:Label("F5 - Show/Hide Menu")
InfoSection:Label("Right Mouse - Aimbot")

-- // Functions \\ --
function UpdateFly()
    if Settings.Fly.BodyVelocity then
        Settings.Fly.BodyVelocity:Destroy()
        Settings.Fly.BodyVelocity = nil
    end
    
    if Settings.Fly.Connection then
        Settings.Fly.Connection:Disconnect()
        Settings.Fly.Connection = nil
    end
    
    if Settings.Fly.Enabled then
        Settings.Fly.BodyVelocity = Instance.new("BodyVelocity")
        Settings.Fly.BodyVelocity.Velocity = Vector3.new(0, 0, 0)
        Settings.Fly.BodyVelocity.MaxForce = Vector3.new(0, 0, 0)
        Settings.Fly.BodyVelocity.Parent = RootPart

        Settings.Fly.Connection = RunService.Heartbeat:Connect(function()
            if not Settings.Fly.Enabled or not Character or not RootPart then
                if Settings.Fly.BodyVelocity then
                    Settings.Fly.BodyVelocity:Destroy()
                    Settings.Fly.BodyVelocity = nil
                end
                return
            end

            local direction = Vector3.new()
            
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then
                direction = direction + Camera.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then
                direction = direction - Camera.CFrame.LookVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then
                direction = direction - Camera.CFrame.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then
                direction = direction + Camera.CFrame.RightVector
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
                direction = direction + Vector3.new(0, 1, 0)
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
                direction = direction - Vector3.new(0, 1, 0)
            end

            direction = direction.Unit * Settings.Fly.Speed
            Settings.Fly.BodyVelocity.Velocity = direction
            Settings.Fly.BodyVelocity.MaxForce = Vector3.new(10000, 10000, 10000)
        end)
    end
end

-- ESP Functions
local ESPFolders = {}

function CreateESP()
    ClearESP()
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            if not Settings.ESP.TeamCheck or player.Team ~= LocalPlayer.Team then
                CreatePlayerESP(player)
            end
        end
    end
end

function CreatePlayerESP(player)
    local folder = Instance.new("Folder")
    folder.Name = player.Name
    folder.Parent = game:GetService("CoreGui")
    ESPFolders[player] = folder
    
    -- Name label
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Name = "Name"
    nameLabel.BackgroundTransparency = 1
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.TextStrokeTransparency = 0
    nameLabel.Text = player.Name
    nameLabel.Visible = Settings.ESP.ShowNames
    nameLabel.Parent = folder
    
    -- Distance label
    local distanceLabel = Instance.new("TextLabel")
    distanceLabel.Name = "Distance"
    distanceLabel.BackgroundTransparency = 1
    distanceLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    distanceLabel.TextStrokeTransparency = 0
    distanceLabel.Visible = Settings.ESP.ShowDistance
    distanceLabel.Parent = folder
    
    -- Health label
    local healthLabel = Instance.new("TextLabel")
    healthLabel.Name = "Health"
    healthLabel.BackgroundTransparency = 1
    healthLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    healthLabel.TextStrokeTransparency = 0
    healthLabel.Visible = Settings.ESP.ShowHealth
    healthLabel.Parent = folder
end

function ClearESP()
    for _, folder in pairs(ESPFolders) do
        folder:Destroy()
    end
    ESPFolders = {}
end

function UpdateESPVisuals()
    for player, folder in pairs(ESPFolders) do
        local character = player.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            local rootPart = character.HumanoidRootPart
            local humanoid = character:FindFirstChild("Humanoid")
            
            -- World to screen space
            local position, onScreen = Camera:WorldToViewportPoint(rootPart.Position)
            
            if onScreen then
                -- Name label
                local nameLabel = folder:FindFirstChild("Name")
                if nameLabel then
                    nameLabel.Position = UDim2.new(0, position.X, 0, position.Y - 30)
                    nameLabel.Visible = Settings.ESP.ShowNames
                end
                
                -- Distance label
                local distanceLabel = folder:FindFirstChild("Distance")
                if distanceLabel then
                    local distance = (rootPart.Position - Camera.CFrame.Position).Magnitude
                    distanceLabel.Text = "Distance: " .. math.floor(distance) .. "m"
                    distanceLabel.Position = UDim2.new(0, position.X, 0, position.Y - 15)
                    distanceLabel.Visible = Settings.ESP.ShowDistance
                end
                
                -- Health label
                local healthLabel = folder:FindFirstChild("Health")
                if healthLabel and humanoid then
                    healthLabel.Text = "HP: " .. math.floor(humanoid.Health) .. "/" .. math.floor(humanoid.MaxHealth)
                    healthLabel.Position = UDim2.new(0, position.X, 0, position.Y)
                    healthLabel.Visible = Settings.ESP.ShowHealth
                end
            else
                for _, child in ipairs(folder:GetChildren()) do
                    child.Visible = false
                end
            end
        else
            for _, child in ipairs(folder:GetChildren()) do
                child.Visible = false
            end
        end
    end
end

-- Aimbot Function
function FindTarget()
    local closestTarget = nil
    local closestDistance = Settings.Aimbot.FOV
    local mousePos = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            if Settings.Aimbot.TeamCheck and player.Team == LocalPlayer.Team then
                continue
            end
            
            local character = player.Character
            local targetPart = character:FindFirstChild(Settings.Aimbot.TargetPart)
            if targetPart then
                local position, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                
                if onScreen then
                    if Settings.Aimbot.VisibleCheck then
                        -- Raycast for visibility check
                        local rayOrigin = Camera.CFrame.Position
                        local rayDirection = (targetPart.Position - rayOrigin).Unit * 1000
                        local raycastParams = RaycastParams.new()
                        raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
                        raycastParams.FilterDescendantsInstances = {LocalPlayer.Character}
                        local raycastResult = Workspace:Raycast(rayOrigin, rayDirection, raycastParams)
                        
                        if raycastResult and raycastResult.Instance:IsDescendantOf(character) then
                            local distance = (Vector2.new(position.X, position.Y) - mousePos).Magnitude
                            
                            if distance < closestDistance then
                                closestDistance = distance
                                closestTarget = targetPart
                            end
                        end
                    else
                        local distance = (Vector2.new(position.X, position.Y) - mousePos).Magnitude
                        
                        if distance < closestDistance then
                            closestDistance = distance
                            closestTarget = targetPart
                        end
                    end
                end
            end
        end
    end
    
    return closestTarget
end

-- Mouse moverel function
function mousemoverel(x, y)
    local currentPos = UserInputService:GetMouseLocation()
    UserInputService:SetMouseLocation(currentPos.X + x, currentPos.Y + y)
end

-- // Connections \\ --
-- Infinite Jump
UserInputService.JumpRequest:Connect(function()
    if Settings.InfiniteJump.Enabled and Humanoid then
        Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- Noclip
RunService.Stepped:Connect(function()
    if Settings.Noclip.Enabled and Character then
        for _, part in pairs(Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end)

-- Aimbot
RunService.RenderStepped:Connect(function()
    if Settings.Aimbot.Enabled and UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
        local target = FindTarget()
        if target then
            local targetPosition = Camera:WorldToViewportPoint(target.Position)
            local mousePosition = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
            local targetPosition2D = Vector2.new(targetPosition.X, targetPosition.Y)
            
            local delta = (targetPosition2D - mousePosition) / Settings.Aimbot.Smoothness
            mousemoverel(delta.X, delta.Y)
        end
    end
end)

-- ESP
RunService.RenderStepped:Connect(function()
    UpdateESPVisuals()
end)

-- Character Added
LocalPlayer.CharacterAdded:Connect(function(newCharacter)
    Character = newCharacter
    Humanoid = Character:WaitForChild("Humanoid")
    RootPart = Character:WaitForChild("HumanoidRootPart")
    
    -- Save original properties
    Settings.SpeedHack.OriginalWalkSpeed = Humanoid.WalkSpeed
    Settings.SpeedHack.OriginalJumpPower = Humanoid.JumpPower
    
    -- Reapply settings if they were enabled
    if Settings.SpeedHack.Enabled then
        Humanoid.WalkSpeed = Settings.SpeedHack.OriginalWalkSpeed * Settings.SpeedHack.Multiplier
        Humanoid.JumpPower = Settings.SpeedHack.OriginalJumpPower * 1.5
    end
    
    if Settings.Fly.Enabled then
        UpdateFly()
    end
    
    if Settings.InfiniteJump.Enabled then
        Humanoid.JumpPower = Settings.InfiniteJump.JumpPower
    end
    
    if Settings.ESP.Enabled then
        CreateESP()
    end
end)

-- Player Added/Removed
Players.PlayerAdded:Connect(function(player)
    if Settings.ESP.Enabled then
        if not Settings.ESP.TeamCheck or player.Team ~= LocalPlayer.Team then
            CreatePlayerESP(player)
        end
    end
end)

Players.PlayerRemoving:Connect(function(player)
    if ESPFolders[player] then
        ESPFolders[player]:Destroy()
        ESPFolders[player] = nil
    end
end)

-- Initialize ESP if enabled
if Settings.ESP.Enabled then
    CreateESP()
end

print("Test Cheat Menu Loaded!")
print("F5 - Show/Hide Menu")
print("Right Mouse Button - Aimbot")
